import React from "react";

import MainPage from "../layouts/main-page";
import {Auth} from "./auth";

const App = () => (
        <Auth>
            <MainPage/>}
        </Auth>
    );

export default App;